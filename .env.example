# Django
DEBUG=True
SECRET_KEY=replace-with-local-secret-key
ALLOWED_HOSTS=127.0.0.1,localhost

# Database (for sqlite local dev leave empty)
DATABASE_URL=

# Email
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True

# Optional app settings
TIME_ZONE=Asia/Calcutta
